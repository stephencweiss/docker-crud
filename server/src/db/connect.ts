// import {Client, ClientConfig} from 'pg';


// const config: ClientConfig = {

// }
export const createConnection = (): Promise<unknown> => {

  return new Promise((res) => setTimeout(() => res, 1000));
  // const client = new Client()
  // return
};